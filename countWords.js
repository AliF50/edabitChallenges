//The function called countWords will take a string. This string will be a sentance, your job is to get the string (sentence) word count
// from the value being passed through the function. Below are some examples of the string's and returned word count's.
function countWords(str){
  return str.split(' ').length;
}
